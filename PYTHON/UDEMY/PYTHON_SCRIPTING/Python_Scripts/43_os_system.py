# os.system module - Used to execute as commands
#                  - Enables the user to execute the system commands with the help of python


'''
TERMINAL:
>>> import os
>>> os.system("cls")
0
>>>

Note: Here, it returns the value ), if the execution is proper, if it returns a non-zero value, then there is error.

status=os.system("cls") ---> when we run this code, the status code gets stored in the variable (Status code - non-zero - Successful)
'''



